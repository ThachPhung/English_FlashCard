# Để trống để dùng proxy Vite (mặc định /api → port 8080)
VITE_API_URL=

# Hoặc trỏ trực tiếp tới backend:
# VITE_API_URL=http://localhost:8080/api

# Port proxy khi chạy npm run dev (nếu backend dùng port khác):
# VITE_API_PROXY=http://127.0.0.1:8080
